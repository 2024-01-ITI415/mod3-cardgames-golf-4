<xml>
    <!-- This file holds info for laying out the Golf Solitaire card game. -->
    
    <!-- The multiplier is multiplied by the x and y attributes below. -->
    <!-- This determines how loose or tight the layout is. --> 
    <multiplier x="1.25" y="1.5" />
    
    <!-- In the XML below, id is the number of the card -->
    <!-- x and y set position -->
    <!-- faceup is 1 if the card is face-up -->
    <!-- layer sets the depth layer so cards overlap properly -->
    <!-- hiddenby is the ids of cards that keep a card face-down -->
    
    <!-- Layer0, the deepest cards. -->
    <slot id="0" x="-4" y="-4" faceup="0" layer="0" hiddenby="1,2" />
    <slot id="1" x="0"   y="-4" faceup="0" layer="0" hiddenby="3,4" />
    <slot id="2" x="4"   y="-4" faceup="0" layer="0" hiddenby="5,6" />
    
    <!-- Layer1, the next level. -->
    <slot id="3" x="-5" y="-2" faceup="0" layer="1" hiddenby="7,8" />
    <slot id="4" x="-1" y="-2" faceup="0" layer="1" hiddenby="8,9" />
    <slot id="5" x="3"   y="-2" faceup="0" layer="1" hiddenby="9,10" />
    
    <!-- Layer2, the next level. -->
    <slot id="6" x="-6" y="0" faceup="0" layer="2" hiddenby="11,12" />
    <slot id="7" x="-2" y="0" faceup="0" layer="2" hiddenby="12,13" />
    <slot id="8" x="2"   y="0" faceup="0" layer="2" hiddenby="13,14" />
    <slot id="9" x="6"   y="0" faceup="0" layer="2" hiddenby="14,15" />
    
    <!-- Layer3, the top level. -->
    <slot id="10" x="-7" y="2" faceup="1" layer="3" />
    <slot id="11" x="-3" y="2" faceup="1" layer="3" />
    <slot id="12" x="1"   y="2" faceup="1" layer="3" />
    <slot id="13" x="5"   y="2" faceup="1" layer="3" />
    
    <!-- This positions the draw pile and staggers it -->
    <slot type="drawpile" x="4" y="4" xstagger="0.15" layer="4"/>
    
    <!-- This positions the discard pile and target card -->
    <slot type="discardpile" x="0" y="0" layer="5"/>
</xml>
